{ ... }: {
	# ===========================================================================
	# QUTEBROWSER (A KEYBOARD-FOCUSED BROWSER WITH A MINIMALIST UI)
	# ===========================================================================
	programs.qutebrowser = {
		enable = true;
	};
}